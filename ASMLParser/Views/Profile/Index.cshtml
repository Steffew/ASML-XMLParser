@model UserViewModel;
<link rel="stylesheet" href="~/css/profile.css" asp-asp-append-version="true" />
<div class="row profile-div">
    <div class="col-9">
        <h3>Profile information</h3>
        <div class="profile-part">
            <label class="fw-bold">
                Naam
            </label>
            <span>
                @Model.Name
            </span>
        </div>
        <div class="profile-part">
            <label class="fw-bold">
                Role
            </label>
            <span>
                @Model.Role.Name
            </span>
        </div>
    </div>
    <div class="col-3">
        <button type="button" class="btn btn-outline-primary profile-button">Edit</button>
    </div>
</div>
@if(Model.Role.Name == "Admin")
{
    <div class="row admin-div">
        <div class="col">
            <h3>Admin information</h3>
            <p>Bla bla admin dingen</p>
        </div>
    </div>
}